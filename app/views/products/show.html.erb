<h1>Your chosen product</h1>
<%= @product.description %>
<% @product.all_photos.each do |photo| %>
<%= image_tag photo.image %>
<% end %>
<%= @product.price %>

<%= current_user ? (cartbutton):(link_to "buy?", new_user_registration_path) %>
